<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
  <div class="container-fluid">
    <a class="navbar-brand" href="#">Navbar</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarColor01">
      <ul class="navbar-nav me-auto">
        <li class="nav-item">
          <%= link_to "Inicio", root_path, class: "nav-link" %>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="#">Pricing</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="#">About</a>
        </li>
        <% if user_signed_in? %>
          <%= link_to "Cerrar sesiòn", destroy_user_session_path, method: :delete, class: "nav-link" %>
        <% else %>
          <%= link_to "Iniciar sesiòn", new_user_session_path, class: "nav-link" %>
          <%= link_to "Registrarse", new_user_registration_path, class: "nav-link" %>
        <% end %>
      </ul>


      <%= form_tag articles_search_path, :method => 'get', class: "d-flex" do %>
        <%= text_field_tag :titulo, params[:titulo], class: "form-control me-sm-2" %>
        <%= submit_tag "Search", :name => nil, class: "btn btn-secondary my-2 my-sm-0" %>
      <% end %>
    </div>
  </div>
</nav>