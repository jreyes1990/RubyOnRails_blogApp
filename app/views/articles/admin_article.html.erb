  <div class="jumbotron">
    <div class="table-responsive-sm">
      <h1 class="text-center">Administrador</h1>
      <%= link_to "Normal", articles_path(normal: true), class: "btn btn-success salto" %>
      <table class="table table-sm table-bordered table-striped table-hover">
        <thead class="thead-dark">
        <tr>
          <th>Titulo</th>
          <th>Cuerpo</th>
          <th>Autor</th>
          <th>Fecha Creacion</th>
          <th colspan="3" class="align-text-bottom"></th>
        </tr>
        </thead>
        <tbody>
        <% @articulos.each do |art| %>
        <tr>
          <td><%= art.title %></td>
          <td><%= truncate(art.body, length: 100) %></td>
          <td><%= art.user.try(:email) %></td>
          <td><%= art.created_at %></td>
          <td><%= link_to "Mostrar", articles_path, class: "btn btn-info" %></td>
          <td><%= link_to "Editar", edit_article_path(art), class: "btn btn-warning" %></td>
          <td><%= link_to "Eliminar", art, method: :delete, class: "btn btn-danger" %></td>
        </tr>
        <% end %>
        </tbody>
      </table>
    </div>
  </div>