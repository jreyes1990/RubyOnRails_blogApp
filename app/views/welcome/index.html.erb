<% @articulos.each do |art| %>
  <div class="jumbotron color-a">
    <h1 class="text-center">
      <%= link_to art.title, art, class: "color-titulo-text" %>
    </h1>
    <p class="text-center"><%= art.body %></p>
  </div>
<% end %>