<div class="jumbotron color-d search-jumbotron">
  <div class="row">
    <% @articulos.each do |art| %>
      <div class="col-md-6">
        <h1><%= link_to art.title, art, class: "search-title" %></h1>
        <p class="search-body"><%= truncate(art.body, length: 300) %></p>
        <p class="text-right"><%= link_to "Ver màs", art, class: "btn btn-success" %></p>
      </div>
    <% end %>
  </div>
</div>